# Recurring jobs configuration for Sidekiq (using sidekiq-cron or similar)
# This file is for Solid Queue recurring jobs, which we're not using.
# For Sidekiq recurring jobs, use sidekiq-cron gem and configure in config/initializers/sidekiq.rb

# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day
